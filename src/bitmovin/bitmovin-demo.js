import { main } from '../main';
import { BitmovinController } from "./bitmovin-controller";

/**
 * Demonstrates the use of the client-side IMA SDK in conjunction with the Bitmovin player, which manages the
 * video player / ad player switching out of the box.
 */
main(BitmovinController);
